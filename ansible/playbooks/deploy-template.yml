# Install Elixir deploy-template
#
# Run setup tasks, e.g. installing packages and creating directories. Run this from an admin user with sudo.
#
# ansible-playbook -u $USER -v -l app-servers playbooks/deploy-template.yml --skip-tags deploy -D
#
# Deploy the code. Run this from the app user account on a dev machine.
#
# ansible-playbook -u deploy -v -l app-servers playbooks/deploy-template.yml --tags deploy --extra-vars ansible_become=false -D
#

- hosts: '*'
  vars:
    app_user: foo
    app_group: foo
  roles:
    - deploy-template
